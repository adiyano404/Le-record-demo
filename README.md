cd /path/to/your/le-record-demo   # folder that contains index.html
git init
git add index.html
git commit -m "Add Le Record demo"
git branch -M main
git remote add origin https://github.com/adiyano404/le-record-demo.git
git push -u origin main
